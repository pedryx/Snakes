<Page x:Class="Snakes_Client.MenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Snakes_Client"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="MenuPage" Background="DarkGreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Vertical" Grid.Column="0">
            <Button Name="localButton" Content="Local" MinHeight="60" Margin="15" FontSize="30" Click="LocalButton_Click"/>
            <Button Name="hostButton" Content="Host" MinHeight="60" Margin="15" FontSize="30" Click="HostButton_Click"/>
            <Button Name="connectButton" Content="Connect" MinHeight="60" Margin="15" FontSize="30" Click="ConnectButton_Click"/>
            <Button Name="foodsButton"  Content="Foods" MinHeight="60" Margin="15" FontSize="30" Click="FoodsButton_Click"/>
        </StackPanel>
        <Button Content="Exit" Height="60" Margin="15" FontSize="30" Click="ExitButton_Click" VerticalAlignment="Bottom"/>
        <Image Name="menuImage" Grid.Column="1" Margin="10"/>
    </Grid>
</Page>
